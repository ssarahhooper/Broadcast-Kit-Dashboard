{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd kit_dashboard && python manage.py collectstatic --noinput && python manage.py migrate --noinput && gunicorn kit_dashboard.wsgi:application",
    "restartPolicyType": "ON_FAILURE"
  }
}